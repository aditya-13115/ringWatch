[project]
name = "ringwatch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "faker>=40.37.0",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
]
